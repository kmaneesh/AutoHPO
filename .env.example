# Meilisearch (run with app: docker compose up)
MEILISEARCH_URL=http://localhost:7700
MEILI_MASTER_KEY=masterKey

# HPO vector search. ENABLE_EMBEDDING=false → keyword-only when running load_hpo
ENABLE_EMBEDDING=true
HPO_EMBEDDING_DIMENSIONS=384
HPO_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Agent: LLM (OpenAI-compatible). Set OPENAI_BASE_URL to your LLM server. In Docker set HOST_IP so container can reach host.
HOST_IP=100.74.210.70
OPENAI_BASE_URL=http://100.74.210.70:1234/v1
OPENAI_MODEL_ID=qwen2.5-7b-instruct-1m
OPENAI_API_KEY=NA

# Agent chat history (SQLite). In Docker use /app/data/agent.db so it persists in mounted volume.
AGENT_DB_FILE=data/agent.db
